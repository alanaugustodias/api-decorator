export enum RouteMethods {
    all = 'all',
    get = 'get',
    post = 'post',
    put = 'put',
    delete = 'delete',
    patch = 'patch',
    options = 'options',
    head = 'head',
}

export enum RouteParamType {
    BODY = 'BODY',
    QUERY = 'QUERY',
    PATH = 'PATH',
    REQUEST = 'REQUEST',
    RESPONSE = 'RESPONSE',
}
